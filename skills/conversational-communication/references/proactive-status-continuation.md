# Proactive status continuation

When the user asks why authorized work has not run, answer the status in one short sentence and continue the available work in the same turn. Do not stop after explaining the delay or make the user probe again to trigger each obvious next step.

Keep the final update short: result, blocker if any, and artifact link only when useful.
