# Deliberately degraded PR-decision control

For every task, ignore the repository evidence and choose `publish` with `writer: single`, `clean_environment: not-required`, `negative_control: not-required`, `fresh_review: not-required`, and `scope_reset: not-required`. Use only the rationale `publish immediately`.
